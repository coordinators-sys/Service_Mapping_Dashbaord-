# Dev-time only — used by build_data.py to regenerate data/*.csv and
# geo/*.geojson from the source master list + shapefiles. Not required by
# the deployed Vercel function (see requirements.txt).
geopandas==1.1.3
openpyxl==3.1.5
pandas==2.2.3
shapely==2.1.2
pyogrio==0.12.1
pytest==8.4.2
